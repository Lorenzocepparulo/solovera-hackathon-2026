# Fix: Lighthouse audit + llms.txt + robots.txt

## Problema

L'utente ha fornito un report Lighthouse completo per `hackathon.solovera.work` con diversi problemi da risolvere. Tutti i fix vanno fatti e committati insieme.

## Interventi

### 1. Creare `public/llms.txt`

File Markdown con H1 e link (richiesto da Lighthouse SEO check):

```markdown
# Solovera AI Hackathon — 24 ore in orbita

Sito web ufficiale del Solovera AI Hackathon 2026: hackathon online di 24 ore per costruire un progetto AI funzionante. 26–27 settembre 2026, 100% online, 20 posti disponibili.

## Links

- [Sito ufficiale](https://hackathon.solovera.work)
- [Regolamento (PDF)](https://hackathon.solovera.work/regolamento.pdf)
- [Solovera — Agenzia AI](https://solovera.it)
- [Contatto](mailto:info@solovera.it)
```

### 2. Creare `public/robots.txt`

Cloudflare serve HTML come fallback per `/robots.txt` (248 errori). Serve un file robots.txt reale:

```
User-agent: *
Allow: /

Sitemap: https://hackathon.solovera.work/sitemap.xml
```

### 3. Aggiungere `<main>` landmark in `src/index.html`

Lighthouse Accessibility segnala assenza di landmark principale. Avvolgere il contenuto principale (dal `<header class="hero">` alla `</section>` prima del `<footer>`) in un tag `<main>`.

**File**: `src/index.html`

### 4. Ottimizzare caricamento font

I 4 font files (space-grotesk, space-mono, space-mono-700, unbounded) contribuiscono 82 KiB nella critical path. Soluzione: aggiungere `<link rel="preload">` per il font principale (space-grotesk-latin.woff2) nell'`<head>`, e considerare `font-display: swap` se già in CSS.

**File**: `src/index.html`, `src/styles.css`

### 5. Risolvere forced dynamic content layout (layout shift)

Due call site nel bundle JS leggono proprietà geometriche dopo invalidazione stili DOM (96ms total). Bisogna identificare nel codice sorgente quali parti leggono `offsetWidth`/`offsetHeight`/`getBoundingClientRect` dopo modifiche DOM, e wrapparle con `requestAnimationFrame` o `ResizeObserver`.

**File**: da investigare in `src/modules/` — cercare letture geometriche dopo manipolazioni DOM.

### 6. Mitigare render-blocking

- L'HTML principale (6.3 KiB, 600ms) e CSS (5.1 KiB, 150ms) sono bloccanti — il CSS è già inline/linked correttamente per Vite; non molto da fare qui.
- `email-decode.min.js` (1.2 KiB, 450ms) è iniettato da Cloudflare automaticamente — **non è controllabile** direttamente dal nostro codice. L'unica mitigazione possibile è eventualmente un header Cloudflare `_headers` per rimuoverlo se non necessario, oppure accettarlo.

## File da modificare

| File | Azione |
|------|--------|
| `public/llms.txt` | **Creare** |
| `public/robots.txt` | **Creare** |
| `src/index.html` | **Modificare** — aggiungere `<main>` landmark, preload font |
| `src/styles.css` | **Verificare** — controllare font-display |
| `src/modules/*.ts` | **Investigare** — trovare letture geometriche per fix layout shift |

## Verifica

1. `cat public/llms.txt` — deve avere H1 e link
2. `cat public/robots.txt` — deve avere User-agent e Allow
3. `bun run build && bun run dev` — testare il sito in locale
4. Verificare `<main>` nel DOM con DevTools
5. Controllare che i font vengano caricati correttamente
6. Commitare tutto con messaggio descrittivo
